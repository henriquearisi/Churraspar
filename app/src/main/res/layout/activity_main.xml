<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="CHURRASPAR"
        android:textSize="24sp"
        android:textColor="#AD0000"
        android:fontFamily="sans-serif-medium"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp" />

    <EditText
        android:id="@+id/edtHomens"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Número de Homens"
        android:inputType="number"
        android:background="@drawable/rounded_edittext"
        android:padding="12dp"
        android:layout_below="@id/tvTitle"
        android:layout_marginTop="16dp" />

    <EditText
        android:id="@+id/edtMulheres"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Número de Mulheres"
        android:inputType="number"
        android:background="@drawable/rounded_edittext"
        android:padding="12dp"
        android:layout_below="@id/edtHomens"
        android:layout_marginTop="12dp" />

    <EditText
        android:id="@+id/edtCriancas"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Número de Crianças"
        android:inputType="number"
        android:background="@drawable/rounded_edittext"
        android:padding="12dp"
        android:layout_below="@id/edtMulheres"
        android:layout_marginTop="12dp" />

    <Button
        android:id="@+id/btnCalcular"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/edtCriancas"
        android:layout_marginTop="24dp"
        android:background="@drawable/rounded_button"
        android:text="Calcular"
        android:textColor="#FFFFFF" />

    <Button
        android:id="@+id/btnLimpar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnCalcular"
        android:layout_marginTop="8dp"
        android:background="@drawable/rounded_button"
        android:text="Limpar"
        android:textColor="#FFFFFF" />

    <TextView
        android:id="@+id/tvResultados"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Resultados aparecerão aqui"
        android:textSize="16sp"
        android:textColor="#424242"
        android:fontFamily="sans-serif"
        android:layout_below="@id/btnLimpar"
        android:layout_marginTop="16dp" />
</RelativeLayout>
